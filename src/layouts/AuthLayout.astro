---
import MainLayout from './MainLayout.astro';

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<MainLayout title={title}>
  <div class="flex min-h-screen items-center justify-center bg-surface-light px-4 py-10 dark:bg-surface-dark">
    <slot />
  </div>
</MainLayout>
